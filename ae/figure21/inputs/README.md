# Figure 21 small simulator inputs

This directory includes the exact 100 normalized T2I1M query vectors and their
top-32 ground truth. The 1.076 GB HNSW index is excluded from Git; see
`../data/input_manifest.tsv` for its exact hash and `../README.md` for build and
placement instructions.
